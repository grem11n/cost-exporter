apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cost-explorer.fullname" . }}-test-connection"
  labels:
    {{- include "cost-explorer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cost-explorer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
